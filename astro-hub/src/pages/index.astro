---
const slogan = "Silent Systems. Unseen Power.";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Penumbral Monolith</title>
    <style>
      :root {
        --obsidian: #0a0a0d;
        --obsidian-soft: #121217;
        --gold: #c9a241;
        --gold-bright: #f3d487;
        --text: #eae7df;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        font-family: "Inter", "Helvetica Neue", sans-serif;
        background: radial-gradient(circle at 50% 20%, var(--obsidian-soft) 0%, var(--obsidian) 65%);
        color: var(--text);
        letter-spacing: 0.015em;
      }

      .shell {
        max-width: 1080px;
        margin: 0 auto;
        padding: 4rem 1.25rem;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        border: 1px solid rgba(201, 162, 65, 0.35);
        background: rgba(10, 10, 13, 0.7);
        padding: 1rem 1.2rem;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 0.85rem;
      }

      .brand h1 {
        margin: 0;
        font-size: 1.2rem;
        font-weight: 600;
        color: var(--gold-bright);
      }

      .tagline {
        margin: 5rem 0 0;
        font-size: clamp(1.2rem, 2.4vw, 2.2rem);
        font-weight: 300;
      }

      .grid {
        margin-top: 2rem;
        display: grid;
        gap: 1rem;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      }

      .card {
        padding: 1rem;
        border: 1px solid rgba(201, 162, 65, 0.2);
        background: rgba(18, 18, 23, 0.72);
      }

      .card h2 {
        margin: 0 0 0.4rem;
        color: var(--gold-bright);
        font-size: 1rem;
      }

      .card p {
        margin: 0;
        opacity: 0.88;
        font-size: 0.95rem;
      }
    </style>
  </head>
  <body>
    <main class="shell">
      <header>
        <div class="brand">
          <svg width="52" height="52" viewBox="0 0 120 120" role="img" aria-label="Penumbral Monolith Logo">
            <rect x="1" y="1" width="118" height="118" fill="#0b0b10" stroke="#c9a241" stroke-width="2"/>
            <polygon points="60,12 30,100 90,100" fill="#c9a241" />
            <polygon points="60,20 38,94 82,94" fill="#f3d487" opacity="0.45" />
            <line x1="60" y1="12" x2="60" y2="100" stroke="#0b0b10" stroke-width="3" opacity="0.7" />
          </svg>
          <h1>Penumbral Monolith</h1>
        </div>
      </header>

      <p class="tagline">{slogan}</p>

      <section class="grid">
        <article class="card">
          <h2>Keryx Messenger</h2>
          <p>Terminal-grade communications for operators and hidden networks.</p>
        </article>
        <article class="card">
          <h2>Aether-Sight</h2>
          <p>Computer vision services for realtime inference and visual intelligence.</p>
        </article>
        <article class="card">
          <h2>Studio Systems</h2>
          <p>Unified architecture by proxy-routed, containerized service boundaries.</p>
        </article>
      </section>
    </main>
  </body>
</html>
